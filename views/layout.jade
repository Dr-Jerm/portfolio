!!!
html(ng-app="myApp")
    head
        meta(charset='utf8')
        base(href='/')
        title Jeremy Bernstein
        != css('style')
        != js('vendor/angular.min')
        != js('vendor/three')
        != js('vendor/stats.min')
        //- script(src="/js/vendor/angular.min.js")
        != js('app')
        != js('helpers')
        != js('directives')

    body.fill
        include background

        include header
        
        block body

        include footer

        != js('background')